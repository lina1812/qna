$('.vote-errors-<%= @vote.votable_type.downcase %>-<%= @vote.votable_id %>').html('<%= render 'shared/errors', resource: @vote %>');

<% if @vote.persisted? %>
  $('.vote-<%= @vote.votable_type.downcase %>-<%= @vote.votable_id %>').replaceWith('<%= j render 'actions', resource: @vote.votable %>');
  $('form#vote-for-' + voteId).addClass('hidden');
  $('form#vote-against-' + voteId).addClass('hidden');
<% end %>