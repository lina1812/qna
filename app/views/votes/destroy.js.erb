$('.vote-errors-<%= @vote.votable_type.downcase %>-<%= @vote.votable_id %>').html('<%= render 'shared/errors', resource: @vote %>');

<% if !@vote.errors.present? %>
  $('.vote-<%= @vote.votable_type.downcase %>-<%= @vote.votable_id %>').html('<%= j render 'actions', resource: @vote.votable %>');

<% end %>