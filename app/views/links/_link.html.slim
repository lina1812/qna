.link
  -if link.is_a_gist?
    - link.raw_gist.split("\n") do |line|
      p = line
  -else  
    li= link_to link.name, link.url
  - if policy(link).destroy?  
    = link_to 'Delete link',
    link_path(link),
    method: :delete,
    remote: true