$('.comment_<%= @comment.commentable_type %>_<%= @comment.commentable_id %>-errors').html('<%= render 'shared/errors', resource: @comment %>');

<% if @comment.persisted? %>
  $('.new-comment #comment_body').val('');
<% end %>